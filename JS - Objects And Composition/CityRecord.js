function solve(name, population, treasury) {
    const ob = {
        name: name,
        population: population,
        treasury: treasury
    };

    return ob;
}

console.log(solve('Tortuga', 7000, 15000));